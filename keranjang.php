<!DOCTYPE html>
<html>
<head>
    <title>Hasil Unggah Gambar</title>
    <style>


         
        

       
    </style>

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&amp;display=swap" rel="stylesheet">
    <!-- MDB -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.2.0/mdb.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="./css/style.css">
    <!-- Carousel -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <title>Infinite card2 Slider JavaScript | CodingNepal</title>
    

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Fontawesome Link for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">
    <script src="script.js" defer></script>
    <!-- Tawks.To -->
    
</head>
<body>

<!-- Navbar Atas -->
<nav class="navbar navbar-expand-lg navbar-dark nv-atas-color">
  <div class="container">
    <a class="navbar-brand" href="#">
      <img src="path/to/logo.png" alt="Logo" width="30" height="30" class="d-inline-block align-text-top">
      Your Brand
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto">
        <!-- Hapus menu Home, About, dan Services -->
      </ul>
      <form class="d-flex navbar-form">
        <div class="input-group">
          <input class="form-control form-control-lg" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-light" type="submit">Search</button>
        </div>
      </form>
      <!-- Float Login -->
    <!-- Button trigger modal -->
    <!-- Buton Daftar 
    <a type="button" class="btn btn-secondary " href="./daftar.html">
      Daftar
    </a>
  -->
    <button type="button" class="btn btn-login-color" data-bs-toggle="modal" data-bs-target="#exampleModal">
    Login
    </button>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5 text-center mx-auto p-2 fs-2" style="width: 800px;" id="exampleModalLabel">Login</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
          <form>
              <div class="mb-3">
                <label for="exampleInputEmail1" class="form-label">Email address</label>
                <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
              </div>
              <div class="mb-3">
                <label for="exampleInputPassword1" class="form-label">Password</label>
                <input type="password" class="form-control" id="exampleInputPassword1">
              </div>
              <div class="text-center">
                  <a>Belum punya akun? </a><a href="./daftar.html" class="text-primary fs-8">Daftar</a>
              </div>
            </form>
      </div>
      <div class="modal-footer text-center mx-auto">
        <button type="button" class="btn btn-primary">Login</button>
      </div>
    </div>
  </div>
</div>
    </div>
  </div>
</nav>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg nv-color justify-content-end">
      <div class="container  ">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 mx-auto ">
            <li class="nav-item dropdown mx-2">
              <a class="nav-link active dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">Kategori</a>
              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="#">Novel</a></li>
                <li><a class="dropdown-item" href="#">Komedi</a></li>
                <li><a class="dropdown-item" href="#">Religi</a></li>
              </ul>
            </li>
            <li class="nav-item mx-2">
              <a class="nav-link" href="#">Pre Order</a>
            </li>
            <li class="nav-item mx-2">
              <a class="nav-link" href="#">Whistlist</a>
            </li>
            <li class="nav-item dropdown mx-2">
              <a class="nav-link " href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                About
              </a>
            </li>
          </ul>
          <!-- Float Login -->

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5 text-center mx-auto p-2 fs-2" style="width: 800px;" id="exampleModalLabel">Login</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
          <form>
              <div class="mb-3">
                <label for="exampleInputEmail1" class="form-label">Email address</label>
                <input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
              </div>
              <div class="mb-3">
                <label for="exampleInputPassword1" class="form-label">Password</label>
                <input type="password" class="form-control" id="exampleInputPassword1">
              </div>
              <div class="text-center">
                  <a>Belum punya akun? </a><a href="./daftar.html" class="text-primary fs-8">Daftar</a>
              </div>
            </form>
      </div>
      <div class="modal-footer text-center mx-auto">
        <button type="button" class="btn btn-primary">Login</button>
      </div>
    </div>
  </div>
</div> 
        </div>
      </div>
    </nav>
    <!-- Navrbar -->

    
    <div class="main-container-keranjang">
  <div class="isi">
    <p>Produk</p>
    <p>Harga</p>
    <p>Kuantitas</p>
    <p>Total Harga</p>
  </div>
  <?php
  // Fetch the book data from the database
  include './php/koneksi.php';

  // Create a new PDO instance
  $pdo = new PDO("mysql:host=$servername;dbname=$dbname", $username, $password);

  // Prepare the SQL statement
  $stmt = $pdo->prepare("SELECT * FROM tes");

  // Execute the prepared statement
  $stmt->execute();

  // Fetch all rows from the result set
  $keranjang = $stmt->fetchAll(PDO::FETCH_ASSOC);

  // Create an associative array to store unique items
  $uniqueItems = array();

  // Filter out duplicate items
  foreach ($keranjang as $item) {
    $id = $item['id'];

    // Check if the item id already exists in the uniqueItems array
    if (!isset($uniqueItems[$id])) {
      $uniqueItems[$id] = $item;
    }
  }
  ?>

  <div id="keranjang-container">
    <?php foreach ($uniqueItems as $kranjang): ?>
      <div class="container-judul">
        <h3 style="text-align: left;"><?php echo $kranjang['pengarang'];?></h3>
        <hr>
      </div>
      <div class="container-isi">
        <div class="checkbox-container">
          <input type="checkbox" onclick="updateTotal()">
        </div>
        <div class="sub-container-keranjang">
          <img src="<?php echo $kranjang['gambar'];?>">
        </div>
        <div class="sub-container-keranjang">
          <p class="harga">Rp. <?php echo number_format($kranjang['harga'], 0, ".", ".");?></p>
        </div>
        <div class="sub-container-keranjang3">
          <button class="minus-btn">-</button>
          <input type="text" class="quantity-input" value="1">
          <button class="plus-btn">+</button>
        </div>
        <div class="sub-container-keranjang">
          <p class="subtotal">Rp. <?php echo number_format($kranjang['harga'], 0, ".", ".");?></p>
        </div>
      </div>
    <?php endforeach; ?>
  </div>
</div>

<div class="footer-keranjang">
  <div class="container-footer">
    <p id="total-jumlah">Total Jumlah: 0</p>
  </div>
  <div class="container-footer">
    <p id="total-harga">Total Harga: Rp. 0</p>
    
  </div>
  <div class="container-footer">
  <button class ="button-checkout" id="checkout-btn" onclick="checkout()">Checkout</button>
  </div>
</div>

<script>
  function updateTotal() {
    var checkboxes = document.querySelectorAll('.checkbox-container input[type="checkbox"]');
    var totalHarga = 0;
    var totalJumlah = 0;

    checkboxes.forEach(function (checkbox) {
      if (checkbox.checked) {
        var containerIsi = checkbox.closest('.container-isi');
        var subtotal = containerIsi.querySelector('.subtotal').textContent;
        var harga = parseInt(subtotal.replace(/[^0-9]/g, ''));
        totalHarga += harga;

        var quantityInput = containerIsi.querySelector('.quantity-input');
        var jumlah = parseInt(quantityInput.value);
        totalJumlah += jumlah;
      }
    });

    document.getElementById('total-harga').textContent = 'Total Harga: Rp. ' + totalHarga.toLocaleString();
    document.getElementById('total-jumlah').textContent = 'Total Jumlah: ' + totalJumlah;
  }

  function checkout() {
    var checkboxes = document.querySelectorAll('.checkbox-container input[type="checkbox"]');
    var selectedItems = [];

    checkboxes.forEach(function (checkbox) {
      if (checkbox.checked) {
        var containerIsi = checkbox.closest('.container-isi');
        var judul = containerIsi.previousElementSibling.querySelector('h3').textContent;
        var harga = containerIsi.querySelector('.harga').textContent;
        var jumlah = containerIsi.querySelector('.quantity-input').value;
        var subtotal = containerIsi.querySelector('.subtotal').textContent;
        var gambar = containerIsi.querySelector('img').src;

        selectedItems.push({
          judul: judul,
          harga: harga,
          jumlah: jumlah,
          subtotal: subtotal,
          gambar: gambar
        });
      }
    });

    // Redirect to checkout.php with selected items as parameter
    var totalHarga = document.getElementById('total-harga').textContent;
    var harga = parseInt(totalHarga.replace(/[^0-9]/g, ''));
    var queryString = "?total=" + harga;

    if (selectedItems.length > 0) {
      queryString += "&items=" + encodeURIComponent(JSON.stringify(selectedItems));
    }

    window.location.href = "./php/invoice.php" + queryString;
  }

  function formatNumber(number) {
    return number.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".");
  }

  var minusBtns = document.querySelectorAll('.minus-btn');
  var plusBtns = document.querySelectorAll('.plus-btn');
  var quantityInputs = document.querySelectorAll('.quantity-input');
  var subtotalElements = document.querySelectorAll('.subtotal');

  for (var i = 0; i < minusBtns.length; i++) {
    minusBtns[i].addEventListener('click', function () {
      var currentContainer = this.parentNode;
      var quantityInput = currentContainer.querySelector('.quantity-input');
      var subtotalElement = currentContainer.nextElementSibling.querySelector('.subtotal');
      var currentValue = parseInt(quantityInput.value);

      if (currentValue > 1) {
        quantityInput.value = currentValue - 1;
        var harga = parseInt(currentContainer.previousElementSibling.querySelector('.harga').textContent.replace('Rp. ', '').replace('.', ''));
        var subtotal = (currentValue - 1) * harga;
        subtotalElement.textContent = 'Rp. ' + formatNumber(subtotal);
      }
    });
  }

  for (var i = 0; i < plusBtns.length; i++) {
    plusBtns[i].addEventListener('click', function () {
      var currentContainer = this.parentNode;
      var quantityInput = currentContainer.querySelector('.quantity-input');
      var subtotalElement = currentContainer.nextElementSibling.querySelector('.subtotal');
      var currentValue = parseInt(quantityInput.value);

      quantityInput.value = currentValue + 1;
      var harga = parseInt(currentContainer.previousElementSibling.querySelector('.harga').textContent.replace('Rp. ', '').replace('.', ''));
      var subtotal = (currentValue + 1) * harga;
      subtotalElement.textContent = 'Rp. ' + formatNumber(subtotal);
    });
  }
</script>




     </div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
  
</body>
</html>
